version: "3.9"
services:
  app:
    build: .
    container_name: expenseless-app
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development